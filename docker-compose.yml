version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app

  frontend:
    build: .
    ports:
      - "8501:8501"
    command: ["streamlit", "run", "app.py", "--server.address", "0.0.0.0"]
    depends_on:
      - api